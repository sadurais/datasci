Analysis of mtcars dataset to infer the relationship mpg ~ tranmission

# Context:
The Motor Trend, an automobiles magazien company would like the answers for
the following questions, looking at a particular dataset: mtcars.

    1. Is an automatic or manual transmission better for MPG?
    2. Quantify the MPG difference between automatic and manual transmissions

# Basic Data Exploration steps
We will take a look at:
    *the dimension of the dataset
    *how many variables are there and their names
    *how are they correlated to each other (visually with a pairs plot)
       and also numerically with a cor() function call.
```$
in the dataset, its dimension,
and their